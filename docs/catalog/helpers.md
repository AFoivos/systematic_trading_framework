<!-- Το αρχείο παράγεται από scripts/generate_component_catalog_appendices.py. Μην αλλάζετε χειροκίνητα τις registry-backed ενότητες. -->

# Κατάλογος Helpers

Τελευταία ενημέρωση: 2026-07-31

Οι helpers είναι deterministic μετασχηματισμοί πάνω σε στήλες που έχουν ήδη παραχθεί. Τα normalizations εκτελούνται πριν από τα transforms μέσα στο ίδιο feature step, επομένως ένα transform μπορεί να διαβάσει έξοδο normalization αλλά όχι το αντίστροφο. Περιλαμβάνονται και οι 32 YAML-declarable εγγραφές των `TRANSFORM_HELPERS` και `NORMALIZATION_HELPERS`. Οι χαμηλού επιπέδου συναρτήσεις `compute_*`, οι validators και το `apply_feature_helpers` είναι εσωτερικές συναρτήσεις υλοποίησης και όχι ξεχωριστά YAML components.

Ο κατάλογος συνδέεται απευθείας με τα registries: κάθε ενεργό όνομα του κώδικα έχει ξεχωριστή αναλυτική ενότητα, ερμηνεία τιμών, χρονικό συμβόλαιο και πλήρες YAML. Τα ονόματα κώδικα, οι στήλες και οι YAML τιμές παραμένουν στα αγγλικά για να αντιγράφονται αυτούσια· όλο το επεξηγηματικό κείμενο είναι ελληνικό.

Στα YAML, `<required>` σημαίνει υποχρεωτική τιμή που πρέπει να αντικατασταθεί και `<configured>` επιλογή χωρίς ασφαλή καθολική προεπιλογή. Το `null` σημαίνει ότι η λειτουργία είναι προαιρετική ή ότι ο υπολογιστής θα επιλέξει το προεπιλεγμένο όνομα/συμπεριφορά.

## Μετασχηματισμοί

### `affine`

**Τι μετρά και τι πληροφορία δίνει.** Εφαρμόζει γραμμικό μετασχηματισμό `scale * source + offset` σε μία ήδη υπάρχουσα στήλη. Η κλίμακα αλλάζει τη μονάδα ή το πρόσημο και η σταθερά μετατοπίζει το σημείο αναφοράς· δεν προσθέτει νέα πληροφορία, αλλά προσαρμόζει την αριθμητική αναπαράσταση για μοντέλα, thresholds ή κοινή κλίμακα.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `scale`, `offset`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    affine:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_pct
        scale: 100.0
        offset: 0.0
        inplace: false
```

### `between_flag`

**Τι μετρά και τι πληροφορία δίνει.** Ελέγχει αν η τιμή μιας στήλης βρίσκεται ανάμεσα σε κατώτερο και ανώτερο όριο. Η έξοδος είναι 1 όταν η συνθήκη ισχύει και 0 διαφορετικά, με ρητή επιλογή για το αν τα δύο άκρα περιλαμβάνονται· είναι χρήσιμο για regime gates και αποδεκτές ζώνες oscillator.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `lower`, `upper`, `inclusive`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    between_flag:
      enabled: true
      params:
        source_col: rsi_14
        source_selector: null
        output_col: rsi_neutral_zone
        lower: 40.0
        upper: 60.0
        inclusive: both
        inplace: false
```

### `crossing_flag`

**Τι μετρά και τι πληροφορία δίνει.** Ανιχνεύει τη στιγμή που μία σειρά διασχίζει αριθμητικό threshold προς τα πάνω ή προς τα κάτω. Η τιμή 1 εμφανίζεται μόνο στο bar της μετάβασης και όχι σε όλα τα bars που παραμένουν πάνω ή κάτω από το όριο, επομένως εκφράζει event και όχι διαρκή κατάσταση.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `threshold`, `direction`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    crossing_flag:
      enabled: true
      params:
        source_col: ppo_hist
        source_selector: null
        output_col: ppo_cross_up
        threshold: 0.0
        direction: up
        inplace: false
```

### `difference`

**Τι μετρά και τι πληροφορία δίνει.** Μετρά τη διαφορά μιας στήλης από παλαιότερη τιμή της ή από δεύτερη στήλη στο ίδιο timestamp. Θετική τιμή σημαίνει αύξηση ή υπεροχή έναντι του reference, αρνητική μείωση ή υστέρηση και μηδέν ισότητα· παρέχει delta, spread ή απλό slope proxy στην αρχική μονάδα.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `reference_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `reference_col`, `output_col`, `periods`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    difference:
      enabled: true
      params:
        source_col: close
        source_selector: null
        reference_col: null
        output_col: close_change_1
        periods: 1
        inplace: false
```

### `lag`

**Τι μετρά και τι πληροφορία δίνει.** Μεταφέρει παλαιότερη τιμή μίας στήλης στο τρέχον row χωρίς να αλλάζει τη σειρά του index. Το αποτέλεσμα στο `t` ισούται με την πηγή στο `t-lag`, προσφέροντας ρητή μνήμη σε tabular μοντέλα και ασφαλή χρονική καθυστέρηση για signals ή συγκρίσεις.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `lag`, `prefix`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    lag:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_lag_1
        lag: 1
        prefix: lag
        inplace: false
```

### `log`

**Τι μετρά και τι πληροφορία δίνει.** Εφαρμόζει φυσικό λογάριθμο στην πηγή μετά από προαιρετική μετατόπιση και προστασία ελάχιστης θετικής τιμής. Συμπιέζει μεγάλες θετικές τιμές και μετατρέπει πολλαπλασιαστικές σχέσεις σε προσθετικές· απαιτεί θετικό `source + offset`, ενώ μη έγκυρες τιμές προστατεύονται από το `eps`.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `offset`, `eps`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    log:
      enabled: true
      params:
        source_col: volume
        source_selector: null
        output_col: log_volume
        offset: 1.0
        eps: 1.0e-12
        inplace: false
```

### `product`

**Τι μετρά και τι πληροφορία δίνει.** Πολλαπλασιάζει δύο point-in-time στήλες και προαιρετικά κλιμακώνει το γινόμενο. Το πρόσημο δείχνει αν οι δύο παράγοντες συμφωνούν ή αντιτίθενται και το μέγεθος τη συνδυασμένη έντασή τους· είναι χρήσιμο για interaction features, όπως κατεύθυνση επί conviction.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `left_col`, `right_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `left_col`, `left_selector`, `right_col`, `right_selector`, `output_col`, `scale`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    product:
      enabled: true
      params:
        left_col: signal_side
        left_selector: null
        right_col: model_conviction
        right_selector: null
        output_col: signed_conviction
        scale: 1.0
        inplace: false
```

### `ratio`

**Τι μετρά και τι πληροφορία δίνει.** Υπολογίζει λόγο αριθμητή προς παρονομαστή, με προαιρετική αφαίρεση σταθεράς και προστασία από μηδενικό παρονομαστή. Χωρίς αφαίρεση η τιμή 1 σημαίνει ισότητα, ενώ με `subtract: 1` το 0 σημαίνει ισότητα και 0,02 σημαίνει ότι ο αριθμητής είναι περίπου 2% υψηλότερος.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `numerator_col`, `denominator_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `numerator_col`, `numerator_selector`, `denominator_col`, `denominator_selector`, `output_col`, `eps`, `subtract`, `denominator_offset`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    ratio:
      enabled: true
      params:
        numerator_col: close
        numerator_selector: null
        denominator_col: ema_50
        denominator_selector: null
        output_col: close_over_ema_50
        eps: 1.0e-08
        subtract: 1.0
        denominator_offset: 0.0
        inplace: false
```

### `reciprocal`

**Τι μετρά και τι πληροφορία δίνει.** Υπολογίζει το αντίστροφο `1/source` ή `1/abs(source)` μιας στήλης. Μεγαλύτερη αρχική τιμή δίνει μικρότερο αντίστροφο· χρησιμοποιείται για μετατροπή συχνότητας σε περίοδο ή κινδύνου σε αντίστροφη κλίμακα, ενώ τιμές κοντά στο μηδέν γίνονται μη έγκυρες μέσω `eps`.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `eps`, `use_abs`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    reciprocal:
      enabled: true
      params:
        source_col: hilbert_frequency_64
        source_selector: null
        output_col: dominant_cycle_64
        eps: 1.0e-12
        use_abs: true
        inplace: false
```

### `rolling_clip`

**Τι μετρά και τι πληροφορία δίνει.** Περιορίζει την τρέχουσα τιμή ανάμεσα σε trailing ποσοστημόρια της πρόσφατης ιστορίας. Μειώνει την επιρροή ακραίων outliers χωρίς να επιβάλλει σταθερά καθολικά όρια· με `shift: 1` τα quantiles εκτιμώνται έως το `t-1`, άρα η τρέχουσα παρατήρηση δεν καθορίζει τα δικά της όρια.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `window`, `lower_q`, `upper_q`, `shift`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rolling_clip:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_clipped
        window: 2520
        lower_q: 0.01
        upper_q: 0.99
        shift: 1
        inplace: false
```

### `rolling_linear_regression`

**Τι μετρά και τι πληροφορία δίνει.** Προσαρμόζει γραμμική παλινδρόμηση της πηγής ως προς τον χρόνο σε κάθε trailing παράθυρο. Η κλίση μετρά κατεύθυνση και ταχύτητα, η τομή το εκτιμώμενο επίπεδο και το R² πόσο καθαρά γραμμική είναι η πρόσφατη διαδρομή· υψηλό R² δηλώνει πιο συνεκτική τάση.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `slope_col`, `intercept_col`, `r2_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `window`, `slope_col`, `intercept_col`, `r2_col`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rolling_linear_regression:
      enabled: true
      params:
        source_col: close
        source_selector: null
        window: 96
        slope_col: close_slope_96
        intercept_col: close_intercept_96
        r2_col: close_r2_96
        inplace: false
```

### `rolling_mean`

**Τι μετρά και τι πληροφορία δίνει.** Υπολογίζει τον κυλιόμενο αριθμητικό μέσο μιας στήλης. Η έξοδος αποτελεί τοπικό baseline ή εξομαλυμένη εκτίμηση επιπέδου· η διαφορά της τρέχουσας τιμής από αυτόν δείχνει σχετική απόκλιση και το `shift` καθορίζει αν συμμετέχει το current bar.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `window`, `min_periods`, `shift`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rolling_mean:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_mean_20
        window: 20
        min_periods: null
        shift: 0
        inplace: false
```

### `rolling_std`

**Τι μετρά και τι πληροφορία δίνει.** Υπολογίζει την κυλιόμενη τυπική απόκλιση μιας στήλης. Μεγαλύτερη τιμή σημαίνει μεγαλύτερη πρόσφατη διασπορά ή αστάθεια και μικρότερη πιο συμπιεσμένη συμπεριφορά· το `ddof` καθορίζει population ή sample convention.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `window`, `min_periods`, `shift`, `ddof`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rolling_std:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_std_20
        window: 20
        min_periods: null
        shift: 0
        ddof: 0
        inplace: false
```

### `rolling_sum`

**Τι μετρά και τι πληροφορία δίνει.** Αθροίζει τις τιμές μιας στήλης μέσα σε trailing παράθυρο. Θετικό ή αρνητικό άθροισμα δείχνει καθαρή συσσώρευση προσημασμένης ροής ή αποδόσεων, ενώ για μη αρνητικές σειρές μετρά συνολική δραστηριότητα στον ορίζοντα.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `window`, `min_periods`, `shift`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rolling_sum:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_sum_20
        window: 20
        min_periods: null
        shift: 0
        inplace: false
```

### `rolling_zscore`

**Τι μετρά και τι πληροφορία δίνει.** Τυποποιεί την τρέχουσα τιμή ως απόσταση από trailing μέσο σε μονάδες trailing τυπικής απόκλισης. Τιμή 0 σημαίνει κοντά στον πρόσφατο μέσο, +2 ασυνήθιστα υψηλή και -2 ασυνήθιστα χαμηλή· το προεπιλεγμένο `shift: 1` αποφεύγει self-inclusion στα statistics.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `window`, `shift`, `ddof`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rolling_zscore:
      enabled: true
      params:
        source_col: close
        source_selector: null
        output_col: close_zscore_2520
        window: 2520
        shift: 1
        ddof: 0
        inplace: false
```

### `rms`

**Τι μετρά και τι πληροφορία δίνει.** Υπολογίζει την τετραγωνική ρίζα του trailing μέσου των τετραγώνων. Μετρά το ενεργειακό ή απόλυτο μέγεθος της σειράς χωρίς ακύρωση θετικών και αρνητικών τιμών· είναι χρήσιμο ως ομαλό amplitude, volatility ή signal-strength measure.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `output_prefix`, `window`, `shift`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rms:
      enabled: true
      params:
        source_col: close_ret
        source_selector: null
        output_col: close_ret_rms_48
        output_prefix: null
        window: 48
        shift: 0
        inplace: false
```

### `rising_flag`

**Τι μετρά και τι πληροφορία δίνει.** Ελέγχει αν η τρέχουσα τιμή είναι υψηλότερη από την τιμή πριν από συγκεκριμένο αριθμό περιόδων. Η έξοδος 1 σημαίνει άνοδο στον επιλεγμένο ορίζοντα και 0 σταθερότητα ή πτώση· δείχνει κατάσταση κατεύθυνσης, όχι μόνο τη στιγμή crossing.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `periods`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    rising_flag:
      enabled: true
      params:
        source_col: ema_50
        source_selector: null
        output_col: ema_50_rising
        periods: 1
        inplace: false
```

### `slope`

**Τι μετρά και τι πληροφορία δίνει.** Μετρά τη μεταβολή της πηγής ανά χρονικό βήμα μέσα στο configured window. Θετική κλίση σημαίνει ανοδική τοπική κατεύθυνση, αρνητική καθοδική και το απόλυτο μέγεθος ταχύτητα στην αρχική μονάδα ανά bar· το `shift` μπορεί να καθυστερήσει το παράθυρο.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `window`, `shift`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    slope:
      enabled: true
      params:
        source_col: close
        source_selector: null
        output_col: close_slope_2
        window: 2
        shift: 0
        inplace: false
```

### `threshold_flag`

**Τι μετρά και τι πληροφορία δίνει.** Συγκρίνει μία στήλη με threshold χρησιμοποιώντας επιλεγμένο τελεστή και προαιρετική απόλυτη τιμή. Η έξοδος είναι 1 όταν η σύγκριση ισχύει και 0 διαφορετικά· σε αντίθεση με το `crossing_flag`, παραμένει ενεργή σε κάθε bar όσο η κατάσταση συνεχίζεται.

**Είσοδοι και έξοδοι.** Το transform helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `source_selector`, `output_col`, `threshold`, `op`, `use_abs`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  transforms:
    threshold_flag:
      enabled: true
      params:
        source_col: atr_over_price_14
        source_selector: null
        output_col: high_atr_flag
        threshold: 0.01
        op: gt
        use_abs: false
        inplace: false
```

## Κανονικοποιήσεις

### `efficiency_ratio`

**Τι μετρά και τι πληροφορία δίνει.** Μετρά την καθαρή μετατόπιση μιας σειράς ως ποσοστό του συνολικού απόλυτου μονοπατιού σε πολλαπλά παράθυρα. Τιμή κοντά στο 1 δείχνει καθαρή κατευθυντική κίνηση και κοντά στο 0 θορυβώδες πήγαινε-έλα· η κανονικοποίηση δεν εξαρτάται από την κλίμακα τιμής.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `windows`, `epsilon`, `bar_minutes`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    efficiency_ratio:
      enabled: true
      params:
        source_col: close
        windows:
        - 24
        - 48
        - 96
        epsilon: 1.0e-12
        bar_minutes: 1.0
        inplace: false
```

### `returns`

**Τι μετρά και τι πληροφορία δίνει.** Παράγει απλές ή λογαριθμικές αποδόσεις για πολλαπλούς trailing ορίζοντες. Θετική τιμή σημαίνει άνοδο από `t-window` έως `t`, αρνητική πτώση και το μέγεθος τη συσσωρευμένη μεταβολή· οι log returns είναι προσθετικές μεταξύ περιόδων.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `close_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `close_col`, `windows`, `log_returns`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    returns:
      enabled: true
      params:
        close_col: close
        windows:
        - 1
        - 4
        - 8
        - 20
        - 48
        log_returns: true
        inplace: false
```

### `atr_distances`

**Τι μετρά και τι πληροφορία δίνει.** Μετατρέπει πολλές configured διαφορές τιμών ή indicators σε μονάδες ATR. Τιμή +1,5 σημαίνει ότι η πρώτη στήλη βρίσκεται 1,5 ATR πάνω από τη δεύτερη και -1,5 αντίστοιχα κάτω, επιτρέποντας σύγκριση assets και volatility regimes.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `atr_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `atr_col`, `pairs`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    atr_distances:
      enabled: true
      params:
        atr_col: atr_14
        pairs:
        - name: close_to_ema_50_atr
          base_col: close
          ref_col: ema_50
        inplace: false
```

### `atr_scaled_distance`

**Τι μετρά και τι πληροφορία δίνει.** Διαιρεί τη διαφορά `base - reference` με μία συγκεκριμένη ATR στήλη. Το πρόσημο δείχνει ποια πλευρά του reference βρίσκεται η βάση και το μέγεθος πόσα τρέχοντα ATR απέχει· αποτελεί scale-free μέτρηση θέσης και επέκτασης.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `base_col`, `ref_col`, `atr_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `base_col`, `ref_col`, `atr_col`, `output_col`, `eps`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    atr_scaled_distance:
      enabled: true
      params:
        base_col: close
        ref_col: ema_50
        atr_col: atr_14
        output_col: close_to_ema_50_atr
        eps: 1.0e-12
        inplace: false
```

### `range_position`

**Τι μετρά και τι πληροφορία δίνει.** Μετρά τη θέση μιας τιμής μέσα στο trailing high–low εύρος. Τιμή 0 αντιστοιχεί στο χαμηλό του παραθύρου, 1 στο υψηλό και 0,5 στο μέσο· με clipping οι υπερβάσεις περιορίζονται στο [0,1], διευκολύνοντας oscillator-style ερμηνεία.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `value_col`, `high_col`, `low_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `value_col`, `high_col`, `low_col`, `window`, `output_col`, `clip`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    range_position:
      enabled: true
      params:
        value_col: close
        high_col: high
        low_col: low
        window: 20
        output_col: close_range_position_20
        clip: true
        inplace: false
```

### `realized_vol_percentile`

**Τι μετρά και τι πληροφορία δίνει.** Μετρά το percentile rank της τρέχουσας realized volatility έναντι trailing ιστορίας. Τιμή 0,90 σημαίνει ότι η μεταβλητότητα είναι υψηλότερη από περίπου το 90% του reference window· με shifted window η τρέχουσα τιμή δεν επηρεάζει τη δική της κατάταξη.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `volatility_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `volatility_col`, `window`, `min_periods`, `output_col`, `shift_window`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    realized_vol_percentile:
      enabled: true
      params:
        volatility_col: vol_rolling_20
        window: 252
        min_periods: null
        output_col: vol_rolling_20_percentile
        shift_window: true
        inplace: false
```

### `robust_zscore`

**Τι μετρά και τι πληροφορία δίνει.** Τυποποιεί μία στήλη με trailing διάμεσο και median absolute deviation αντί για μέσο και τυπική απόκλιση. Τιμές γύρω στο 0 είναι τυπικές, μεγάλες θετικές ή αρνητικές είναι ανθεκτικές ακραίες αποκλίσεις· η μέθοδος επηρεάζεται λιγότερο από outliers και heavy tails.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `window`, `min_periods`, `output_col`, `shift_stats`, `mad_scale`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    robust_zscore:
      enabled: true
      params:
        source_col: close_ret
        window: 252
        min_periods: null
        output_col: close_ret_robust_zscore
        shift_stats: true
        mad_scale: 1.4826
        inplace: false
```

### `rolling_beta_residual`

**Τι μετρά και τι πληροφορία δίνει.** Εκτιμά trailing alpha και beta του asset έναντι benchmark και υπολογίζει το τρέχον residual return. Beta πάνω από 1 σημαίνει μεγαλύτερη ευαισθησία στο benchmark, alpha τοπική σταθερή συνιστώσα και residual την κίνηση που δεν εξηγείται από τη συστηματική έκθεση.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `asset_return_col`, `benchmark_return_col`, `residual_col`, `beta_col`, `alpha_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `asset_return_col`, `benchmark_return_col`, `window`, `min_periods`, `residual_col`, `beta_col`, `alpha_col`, `shift_stats`, `eps`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    rolling_beta_residual:
      enabled: true
      params:
        asset_return_col: close_ret
        benchmark_return_col: benchmark_ret
        window: 252
        min_periods: null
        residual_col: market_residual_return
        beta_col: market_beta_252
        alpha_col: market_alpha_252
        shift_stats: true
        eps: 1.0e-12
        inplace: false
```

### `rolling_percent_rank`

**Τι μετρά και τι πληροφορία δίνει.** Κατατάσσει την τρέχουσα τιμή μέσα στην trailing εμπειρική κατανομή της. Τιμή 0,95 σημαίνει ότι βρίσκεται πάνω από περίπου το 95% της πρόσφατης ιστορίας και 0,05 πολύ χαμηλά· είναι scale-free regime measure χωρίς υπόθεση κανονικότητας.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `source_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `source_col`, `window`, `min_periods`, `output_col`, `shift_window`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    rolling_percent_rank:
      enabled: true
      params:
        source_col: atr_over_price_14
        window: 252
        min_periods: null
        output_col: atr_percent_rank_252
        shift_window: true
        inplace: false
```

### `volatility`

**Τι μετρά και τι πληροφορία δίνει.** Παράγει ATR ως ποσοστό της τιμής και προαιρετικό trailing percentile του ATR. Το ATR percentage επιτρέπει σύγκριση διαφορετικών price scales, ενώ το percentile δείχνει αν το τρέχον επίπεδο κινδύνου είναι υψηλό ή χαμηλό σε σχέση με το πρόσφατο καθεστώς.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `close_col`, `atr_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `close_col`, `atr_col`, `add_atr_pct`, `add_atr_percentile`, `percentile_window`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    volatility:
      enabled: true
      params:
        close_col: close
        atr_col: atr_14
        add_atr_pct: true
        add_atr_percentile: true
        percentile_window: 252
        inplace: false
```

### `volatility_scaled_return`

**Τι μετρά και τι πληροφορία δίνει.** Διαιρεί μία return στήλη με point-in-time volatility measure. Θετική τιμή σημαίνει ανοδική απόδοση ανά μονάδα κινδύνου, αρνητική καθοδική και μεγάλο απόλυτο μέγεθος ασυνήθιστα ισχυρή κίνηση για το τρέχον volatility regime.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `return_col`, `volatility_col`, `output_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `return_col`, `volatility_col`, `output_col`, `eps`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    volatility_scaled_return:
      enabled: true
      params:
        return_col: close_ret
        volatility_col: atr_over_price_14
        output_col: close_ret_over_atr
        eps: 1.0e-12
        inplace: false
```

### `volume_relative`

**Τι μετρά και τι πληροφορία δίνει.** Μετρά τον τρέχοντα όγκο ως λόγο προς trailing μέσο και προαιρετικά ως z-score. Λόγος 2 σημαίνει διπλάσια δραστηριότητα από το πρόσφατο baseline, ενώ θετικό μεγάλο z-score δηλώνει ασυνήθιστα υψηλή συμμετοχή· shifted statistics αποφεύγουν self-inclusion.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των `volume_col`, `output_col`, `zscore_col`. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `volume_col`, `window`, `min_periods`, `output_col`, `zscore_col`, `shift_stats`, `eps`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    volume_relative:
      enabled: true
      params:
        volume_col: volume
        window: 96
        min_periods: null
        output_col: volume_relative_96
        zscore_col: volume_zscore_96
        shift_stats: true
        eps: 1.0e-12
        inplace: false
```

### `rolling_zscores`

**Τι μετρά και τι πληροφορία δίνει.** Εφαρμόζει την ίδια trailing z-score κανονικοποίηση σε λίστα στηλών. Κάθε έξοδος εκφράζει πόσες τυπικές αποκλίσεις απέχει η αντίστοιχη τρέχουσα τιμή από την παρελθοντική της κατανομή, με κοινό window για συνεπή multi-feature scaling.

**Είσοδοι και έξοδοι.** Το normalization helper δηλώνει τις στήλες εισόδου και τα παραμετροποιήσιμα ονόματα εξόδου μέσω των τις σταθερές προεπιλογές του υπολογιστή. Η προηγούμενη παράγραφος εξηγεί την οικονομική σημασία της εξόδου, ενώ το YAML δείχνει τα ακριβή ονόματα στηλών που διαβάζονται ή γράφονται. Όλες οι παράμετροι, μαζί με τις προεπιλογές και τις υποχρεωτικές τιμές, εμφανίζονται παρακάτω.

**Χρονική ορθότητα και αποφυγή διαρροής.** Ο helper επιτρέπεται να χρησιμοποιεί μόνο στήλες που έχουν ήδη παραχθεί στο ίδιο feature step ή σε προηγούμενο στάδιο. Rolling statistics με `shift`, `shift_stats` ή `shift_window` πρέπει να παραμένουν ενεργοποιημένα όταν η τρέχουσα τιμή συγκρίνεται με παρελθοντικό baseline, ώστε να αποφεύγεται η αυτοσυμμετοχή.

**Παράμετροι.** Οι παράμετροι του contract είναι: με προεπιλογή `columns`, `window`, `min_periods`, `shift_stats`, `inplace`.

**Πλήρες YAML παράδειγμα:**

```yaml
features:
- step: <feature_step>
  params: {}
  normalizations:
    rolling_zscores:
      enabled: true
      params:
        columns:
        - close_ret
        - atr_over_price_14
        window: 96
        min_periods: null
        shift_stats: true
        inplace: false
```
